## Competitive Programming Manager

```sh
./run.py -i in.txt -o out.txt  `./compete.py cf790_a.cpp` # start cf round 790 problem A with cpp template
./run.py -i in.txt -o out.txt  `./compete.py _` # next problem with cpp
./run.py -i in.txt -o out.txt  `./compete.py .py` # try current problem with py template
./run.py -i in.txt -o out.txt  `./compete.py _cpp` # next problem with cpp template
./run.py -i in.txt -o out.txt  `./compete.py .` # just run current problem
```